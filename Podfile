# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyFavQuotes' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyFavQuotes
  pod 'Alamofire'
  pod 'SwiftyJSON'
  target 'MyFavQuotesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyFavQuotesUITests' do
    # Pods for testing
  end

end
